const { parse, isAfter, differenceInMinutes, startOfDay } = require('date-fns'); const checkInStr = '2026-06-28T04:55:00Z'; const checkIn = new Date(checkInStr); const date = startOfDay(checkIn); console.log('date:', date); const shiftStart = parse('09:55', 'HH:mm', date); console.log('shiftStart:', shiftStart); const lateAllowMins = 15; const expectedArrival = new Date(shiftStart.getTime() + lateAllowMins * 60000); console.log('expectedArrival:', expectedArrival); console.log('isAfter:', isAfter(checkIn, expectedArrival)); console.log('lateMinutes:', differenceInMinutes(checkIn, shiftStart));
