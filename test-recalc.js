const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const req = new Request('http://localhost/api/recalculate', {method:'POST', body:JSON.stringify({startDate:'2026-06-28', endDate:'2026-06-28'})}); const {POST} = require('./src/app/api/recalculate/route.ts'); const res = await POST(req); console.log(await res.json()); } run().finally(()=>prisma.$disconnect());
